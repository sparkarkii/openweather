name: openweather_workflow

on:
  schedule:
    - cron: "00 08 * * *"    #runs everyday
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: checkout main repo
        uses: actions/checkout@v6.0.2
      - name: checkout utils repo
        uses: actions/checkout@v6.0.2
        with: 
          repository: sparkarkii/my_utilities
          path: my_utilities
      - name: setup python
        uses: actions/setup-python@v6.2.0
        with:
          python-version: 3.14
      - name: install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests 
      - name: execute script
        env:
          EMAIL: ${{ secrets.EMAIL }}
          PW: ${{ secrets.PW }}
          OPENWEATHER_KEY: ${{ secrets.OPENWEATHER_KEY }}
        run: |
          python openweather.py
